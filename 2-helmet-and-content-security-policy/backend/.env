# Cổng lắng nghe của server
# (EN: Server listen port)
APP_PORT=3000

# Bật chế độ report-only cho CSP (true = report-only, false = enforce)
# (EN: Enable CSP report-only mode — true = report-only, false = enforce)
CSP_REPORT_ONLY=true
